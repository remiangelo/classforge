---
import background from '../assets/background.svg';
---

<div id="container">
	<img id="background" src={background.src} alt="" fetchpriority="high" />
	<main>
		<section id="hero">
			<div class="logo-container">
				<h1 class="app-title">ClassForge</h1>
			</div>
			<h2 class="tagline">
				The modern <code><pre>school management system</pre></code> built for the future of education.
			</h2>
			<section id="links">
				<a class="button" href="/login">Sign In</a>
				<a class="button secondary" href="/register">Create Account</a>
			</section>
		</section>
	</main>

	<div id="features" class="box">
		<div class="feature">
			<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
				<path d="M17 3a2.85 2.83 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z"></path>
				<path d="m15 5 4 4"></path>
			</svg>
			<h3>Student Management</h3>
			<p>Easily manage student records, track attendance, and monitor performance.</p>
		</div>
		<div class="feature">
			<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
				<rect width="18" height="18" x="3" y="3" rx="2"></rect>
				<path d="M3 9h18"></path>
				<path d="M9 21V9"></path>
			</svg>
			<h3>Timetable Planning</h3>
			<p>Create and manage class schedules with an intuitive interface.</p>
		</div>
		<div class="feature">
			<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
				<path d="M22 12h-4l-3 9L9 3l-3 9H2"></path>
			</svg>
			<h3>Performance Analytics</h3>
			<p>Get insights into student performance with detailed reports and analytics.</p>
		</div>
	</div>
</div>

<style>
	#background {
		position: fixed;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		z-index: -1;
		filter: blur(100px);
	}

	#container {
		font-family: Inter, Roboto, 'Helvetica Neue', 'Arial Nova', 'Nimbus Sans', Arial, sans-serif;
		height: 100%;
		display: flex;
		flex-direction: column;
	}

	main {
		height: 100%;
		display: flex;
		justify-content: center;
		padding-top: 5rem;
	}

	#hero {
		display: flex;
		align-items: center;
		flex-direction: column;
		justify-content: center;
		padding: 2rem;
		text-align: center;
		max-width: 800px;
	}

	.logo-container {
		margin-bottom: 1rem;
	}

	.app-title {
		font-size: 3rem;
		font-weight: bold;
		color: #3245ff;
		margin: 0;
		background: linear-gradient(83.21deg, #3245ff 0%, #bc52ee 100%);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
	}

	.tagline {
		font-size: 1.5rem;
		margin: 1rem 0 2rem;
		font-weight: normal;
		color: #111827;
		line-height: 1.4;
	}

	#links {
		display: flex;
		gap: 1rem;
		margin-top: 1rem;
	}

	#links a {
		display: flex;
		align-items: center;
		justify-content: center;
		padding: 0.75rem 1.5rem;
		color: white;
		text-decoration: none;
		transition: all 0.2s ease;
		font-weight: 500;
		min-width: 10rem;
	}

	#links a.button {
		color: white;
		background: linear-gradient(83.21deg, #3245ff 0%, #bc52ee 100%);
		box-shadow:
			inset 0 0 0 1px rgba(255, 255, 255, 0.12),
			inset 0 -2px 0 rgba(0, 0, 0, 0.24);
		border-radius: 8px;
	}

	#links a.button:hover {
		transform: translateY(-2px);
		box-shadow: 0 5px 15px rgba(50, 69, 255, 0.2);
	}

	#links a.button.secondary {
		background: white;
		color: #3245ff;
		border: 1px solid #3245ff;
	}

	#links a.button.secondary:hover {
		background: #f5f5ff;
	}

	pre {
		font-family:
			ui-monospace, 'Cascadia Code', 'Source Code Pro', Menlo, Consolas, 'DejaVu Sans Mono',
			monospace;
		font-weight: normal;
		background: linear-gradient(14deg, #d83333 0%, #bc52ee 100%);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		margin: 0;
	}

	code {
		display: inline-block;
		background:
			linear-gradient(66.77deg, #f3cddd 0%, #f5cee7 100%) padding-box,
			linear-gradient(155deg, #d83333 0%, #f041ff 18%, #f5cee7 45%) border-box;
		border-radius: 8px;
		border: 1px solid transparent;
		padding: 6px 8px;
	}

	.box {
		padding: 1.5rem;
		background: rgba(255, 255, 255, 0.9);
		border-radius: 16px;
		border: 1px solid rgba(255, 255, 255, 0.5);
		box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
		backdrop-filter: blur(20px);
		margin: 2rem auto;
		max-width: 1000px;
		width: 90%;
	}

	#features {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 2rem;
	}

	.feature {
		display: flex;
		flex-direction: column;
		align-items: center;
		text-align: center;
	}

	.feature svg {
		color: #3245ff;
		margin-bottom: 1rem;
		width: 2.5rem;
		height: 2.5rem;
	}

	.feature h3 {
		margin: 0 0 0.5rem;
		color: #111827;
		font-size: 1.25rem;
	}

	.feature p {
		color: #4b5563;
		font-size: 1rem;
		line-height: 1.6;
		margin: 0;
	}

	@media screen and (max-width: 768px) {
		#container {
			display: flex;
			flex-direction: column;
		}

		#hero {
			padding: 1rem;
		}

		.app-title {
			font-size: 2.5rem;
		}

		.tagline {
			font-size: 1.25rem;
		}

		#links {
			flex-direction: column;
			width: 100%;
		}

		#links a.button {
			width: 100%;
		}

		#features {
			grid-template-columns: 1fr;
		}
	}
</style>
